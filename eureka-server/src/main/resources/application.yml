application:
  version: 0.0.1-SNAPSHOT
  title: Eureka Server


spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false